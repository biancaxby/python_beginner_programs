#display all numbers that dont have a duplicate
users = []

for i in range(5):
    user = input("enter: ")
    if user in users:
        user.append(users)
   
    if user in users is > 1:
        users.clear()

print(users)
        
